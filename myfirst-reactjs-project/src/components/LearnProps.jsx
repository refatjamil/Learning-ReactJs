export const LearnProps = (props) => {
    return (
        <>
        <h1>Learn Props</h1>
        <p>Name: {props.name}</p>
        <p>Roll: {props.roll}</p>
        ..........................
        </>
    )
} 